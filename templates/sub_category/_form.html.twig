{{ form_start(form) }}
    <label for =''> Nom de la sous-catégorie</label>
    {{ form_widget(form.name, {attr:{'class':'form form-control'}}) }}<br>
    <label for =''> Nom de la sous-catégorie</label>
    {{ form_widget(form.category, {attr:{'class':'form form-control'}}) }}<br>
    <button class="btn btn-outline-success mb-3">{{ button_label|default('Save') }}</button> <br>
{{ form_end(form) }}
